# AUDIT.md — Typa（タイパ）の 監査

GIGA Standard v5 / `/rollout` による 監査と 改修の 記録。

- 監査日：2026-08-04
- 対象：`GIGAyama/Typa`（**A型** — 単一 HTML ＋ `css/` `js/` ＋ PWA 一式。ビルドなし）
- 測りかた：`scripts/measure/` の 3本（実ブラウザ Chromium）と `npm run check`

> **この 文書の 決まりごと**
> 「未計測」は ✅ では ありません。測って いない ものは そのまま「未計測」と 書きます。
> 数字は すべて 実ブラウザでの 実測値です。推測では 書いて いません。

---

## 0. まとめ（前 → 後）

| 測った もの | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満（4画面 × 明暗、全10画面を 走査） | **191件** | **0件** |
| 打って いる とちゅうの コントラスト基準未満 | **4種** | **0件** |
| タップ 44px 未満 | **1件** | **0件** |
| 320px はばの よこスクロール | 0件 | 0件 |
| JS エラー・CSP違反 | 0件 | 0件 |
| CDN から 取る 実行コード | 0 バイト | 0 バイト |
| 版を 上げた ときの ふるまい | **断りなく 即 切替** | **押すまで 待つ** |
| `apple-touch-icon` の 透明 | **42.1%** | **0.0%** |
| maskable の 透明（下地の 余白） | **42.0%** | **0.0%** |
| maskable セーフゾーン外の 中身 | 0.000% | 0.000% |
| Part I の 自動検査 | （無し） | **31件 すべて 通過** |
| 初回表示に 必要な JS（gzip 前） | 479 KB | **479 KB（未達）** |

---

## 1. 実測の しかた（そのまま くりかえせます）

```bash
npm run check              # 静的な 検査（CI と 同じ）
npm run check:self-test    # 検査そのものが 動いて いるかを たしかめる
npm i -D playwright && npx playwright install chromium
npm run measure            # 見た目（4画面 × 明暗、全10画面）
npm run measure:typing     # 打って いる とちゅうにしか 出ない ところ
npm run measure:pwa        # PWA の ふるまい（更新・圏外・他アプリの ひかえ）
```

測った 画面：うつ／えらぶ／コース一覧／チャレンジ／きろく／バッジ／せってい／
ローマ字ひょう／きろくを もちだす／うつ（指の色分け OFF）
測った 画面サイズ：1366×768（あかるい・くらい）／320×568／810×1080

---

## 2. 見つかった もの と 直しかた

### 2-1. コントラスト 191件 → 0件

いちばん 落ちて いたのは **画面の キーボード**でした。
指の 色分けの ために キーの 地に 色を しいて あるのに、
その 上の 小さい 字は「白い 面での 色」として 決めた `--muted` の ままでした。

| どこ | 前 | 後 | 何の 字か |
|---|---:|---:|---|
| キーの いちばん 上の 行（行ごと `opacity: .62`） | **2.48** / くらい画面 **2.88** | 5.1 以上 | 「もどる」「すすむ」「ぜんがめん」「あかるさ ＋」 |
| キーの かな・記号・名まえ（指の 色の 上） | 3.25〜4.44 | **4.59 以上** | 打つ キーの よみ |
| 「つぎに 押す キー」の 文字（指の色分け OFF） | 3.29 | 5.61 | いま 押す キー |
| 「つぎに 押す キー」の 文字（指の色分け ON） | 3.71（実測 4.14） | 4.87 / くらい 5.22 | いま 押す キー |
| ○ 正かいの しるし（くらい画面） | **1.58** | 11.52 | 打った しゅんかん |
| × まちがいの しるし（くらい画面） | **2.21** | 8.25 | 打った しゅんかん |
| ○×の 上の かな | 2.19 | 継承で 解消 | 打った しゅんかん |
| コースの 名まえ（`.btn-sub`、`opacity: .9`） | 4.27 | 5.00 | ボタンの 副題 |

**いちばん ひどかったのは「読めなくて いちばん こまる 人が 読む 字」**でした。
上の行の キーの 名まえ（比 2.48）は、ショートカットの れんしゅうで
実さいに 読む ことばです。行ごと `opacity: .62` を かけて いたため、
「つかわない 行」に 見せる つもりが「読めない 行」に なって いました。

直しかた（Part I §2-8 の「面用と 文字用の 2段階」）：

```css
--kb-ink: #536276;        /* あかるい 画面。9本の 指の 色すべての 上で 最悪 4.59 */
--kb-ink: #b7c1ce;        /* くらい 画面。同 最悪 4.60 */
--kb-next-tint: 72% / 60%;/* くらい 画面は 60% に して 白い 字で 5.22 を 取る */
--kb-hit-ink: #fff / #0b1622;  /* ○×の 面。くらい 画面では こい 字 */
```

**一括置換は して いません。** 変えたのは 変数の 追加と、キーの 中の 6か所だけです。
変えた あと、明暗 × 4画面 × 10画面を もう一度 走査して 0件を たしかめました。

### 2-2. タップ 44px 未満 1件 → 0件

`.btn-stop`（「やめる」）が **104×40**。
たてが みじかい Chromebook 用の `@media` で `min-height: 40px` に つめ、
その ぶんを キーボードに まわして いる ところでした。
ボタンを 大きく すると キーボードが 小さく なるので、
Part I §2-9 の とおり **疑似要素で 当たり判定だけ** 44px に しました（104×44）。

### 2-3. PWA — 更新が 断りなく 入れかわって いた

`sw.js` の `install` で `skipWaiting()` して いました。実測：

| | 前 | 後 |
|---|---|---|
| 版を 上げて 3秒 放置 | **勝手に 切りかわった** | `waiting` の まま ✅ |
| 更新の おしらせ | 無し | 「あたらしい ばんが あります／さいしんに する／あとで」✅ |
| 放置中の 読みこみ直し | — | 0回 ✅ |
| 押した あと | — | 1回だけ 読みこみ直し ✅ |
| 古い `typa-` の ひかえ | — | 消える ✅ |
| 他アプリの ひかえ | 2/2 のこる ✅ | 2/2 のこる ✅ |

児童が 打って いる まっさい中に 入れかわると、打ちかけの お題と
出した ばかりの けっかが 消えます。押されるまで 待つ 形に しました。

`controllerchange` は **押された ときだけ** 受けます。
そのまま 受けると 初回訪問が かならず 1回 リロードされる ためです
（実測：初回訪問の 文書読みこみは 前後 とも 1回で 変わって いません）。

### 2-4. アイコン

| ファイル | 透明（前） | 透明（後） | セーフゾーン外の 中身 |
|---|---:|---:|---:|
| `apple-touch-icon.png` | **42.1%** | **0.0%** | 0.028% |
| `icon-maskable-192.png` | 42.0% | **0.0%** | 0.000% |
| `icon-maskable-512.png` | 41.9% | **0.0%** | 0.000% |

- `apple-touch-icon` の 透明は iOS が 黒で うめます。ホーム画面で **四すみだけが 黒く** 出ます。
- maskable は 図の 欠けは もともと 0.000% でした（Part I §3-7 の パターン1では ない）。
  問題は **パターン2「余白あり」**で、下地が はしまで 届かず **一回り 小さく 見えて** いました。
  下地を 全面に のばしました。`icons/make_icons.py` は 図形から 作りなおすので、
  §3-7 が 注意して いる「単色の グラデーションで 角丸の 輪郭が 影に なる」問題は 起きません。

大きさは ぜんぶ 上限内（`favicon` 0.6KB / 512 は 5.9KB / 合計 18.8KB）。

### 2-5. CSP — 入れて 実さいに 壊れた ところ

`script-src 'self'` は そのまま 通りました（本体は もともと ぜんぶ 外部ファイル、
インライン `<script>` も `onclick=` も 0件）。

**壊れたのは `style-src` でした。**
`style-src 'self'` だけに すると、キーの 指の 色分けを 出して いる
`style="--finger: …"` が 6か所 とも 止まり、コンソールに 27件 出ました。
**画面は ふつうに 出るのに、指の 色分けだけが 黙って 消えます。**
JS エラーには ならないので、走らせないと 気づけません。

`script-src` を しめた ままに して、`style-src` にだけ `'unsafe-inline'` を
足しました（Part I §2-13 の 見本も 同じ 形です）。
入れた あと、全画面 × 明暗で **CSP違反 0件・JS エラー 0件** を たしかめて います。

`frame-ancestors` は 書いて いません。`<meta>` では 無視され、
読みこみの たびに 警告が 出るだけ だからです。
`index.html` に「独自ドメインや CDN を はさむ ときに サーバー側で 足す」旨を
コメントで のこして あります。

### 2-6. 品質ゲートを 「わざと こわして」たしかめた

`npm run check:self-test` は、リポジトリを 一時フォルダに 写して
検査ごとに わざと 1か所 こわし、その 検査が ちゃんと 落ちるかを 見ます。

**この たしかめで、検査そのものの 不具合が 3件 見つかりました。**

| 不具合 | 中身 |
|---|---|
| `E_SW_CACHE_SCOPE` の 誤通過 | ファイル 全体から `startsWith` を さがして いた。`sw.js` には `fetch` の 中に 別の `startsWith` が ふつうに あるので、`caches.keys()` を **全消しして いても 通って いた**。`caches.keys()` から その 式の おわりまで だけを 見るように 直した |
| `E_SW_PRECACHE_OFFLINE` の 誤通過 | ファイル 全体から `offline.html` を さがして いた。逃げ道の `caches.match('./offline.html')` が 見つかるので、**先読みして いなくても 通って いた**。先読みの 配列の 中に あるかを 見るように 直した |
| `D_FLUID_TYPE` の こわしかたの 誤り | `clamp(` を `notclamp(` に 置きかえて いたが、`/clamp\(/` は まだ 当たる。こわした つもりで こわせて いなかった |

前の 2件は 検査そのものの あな です。「0件でした」だけを 見て いたら
気づけませんでした。いまは **31件 すべてが「こわしたら 落ちる」**ことを
たしかめて あります。

---

## 3. 監査表（Part II Phase 7 の A〜G）

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama（今回 追加） |
| A2 | .gitignore | ✅ | node_modules / .env / .clasp.json ほか（今回 追加） |
| A3 | dependabot.yml | ✅ | github-actions を monthly（実行時の 依存は 0個）（今回 追加） |
| A4 | README / MANUAL / AUDIT | ✅ | 3つ とも あり（MANUAL・AUDIT は 今回 追加） |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで 動作確認済み） | ✅ | 全画面 × 明暗で CSP違反 0件・JSエラー 0件。`style-src` の 判断は §2-5 |
| B2 | 秘密情報・IDの 直書きなし | ✅ | `.env` / `.clasp.json` は 履歴にも 無し。通信 0 |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を つかって いません） |
| B4 | postMessage の 宛先が `*` でない | ✅ | `postMessage` は Service Worker への `SKIP_WAITING` 1か所のみ（宛先の 指定が 要らない API） |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーが ありません。個人情報も もちません） |
| B6 | CDN から 取る 実行コードが 0 | ✅ | **0 バイト**。外部を 指す `src`/`href` は 0件 |
| B7 | 残る 外部資産に SRI と 版の固定 | — | 該当なし（外部資産が 0件） |

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS を つかって いません） |
| C2 | 自動復旧 | ✅ | 記録が 壊れて いても 既定値で 起動（`tools/check-backup.js` で 検査） |
| C3 | pagehide で 記録確定 | ✅ | `play.js` / `shortcut.js` の 2か所 |
| C4 | 通信失敗時の リトライと 明示 | — | 該当なし（通信 0） |
| C5 | localStorage.clear() を つかって いない | ✅ | 0件（自アプリ接頭辞のみ 削除） |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | あり |
| D2 | 100dvh を 使用 | ✅ | `min-height: 100dvh` ＋ `@supports not` の ひかえ（今回 追加） |
| D3 | safe-area-inset | ✅ | 5か所（下部バー・左右・トースト・更新の帯） |
| D4 | clamp() による fluid type | ✅ | 14か所 |
| D5 | Canvas の DPR 補正 | — | 該当なし（Canvas を つかわず、図は ぜんぶ インライン SVG） |
| D6 | 320px はばで よこスクロールが 出ない | ✅ | **0件**（10画面 すべて） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` 0件（図は SVG）。PNG は アイコン 6枚のみ、最大 5.9KB |
| D8 | コントラスト 4.5:1 以上（実ブラウザで 全画面を 走査） | ✅ | **191件 → 0件**（4画面サイズ × 明暗）。打って いる とちゅうも 0件 |
| D9 | タップ 44px 以上（::after こみで 実測） | ✅ | **1件 → 0件** |
| D10 | prefers-reduced-motion（.01ms であって 0 でない） | ✅ | `.001ms`。`scroll-behavior: auto` を 追加 |
| D11 | forced-colors 対応 | ✅ | 今回 追加。「つぎに 押す キー」を Highlight で 引きなおす |
| D12 | 提示モード | ❌ | **ありません。**（→ §4「人間に 決めて ほしい こと」） |
| D13 | 印刷CSS | △ | 固定要素を 隠す 最低限のみ 追加。ローマ字ひょうの 紙面設計は して いません（→ §4） |
| D14 | 拡大を 禁止して いない | ✅ | `user-scalable=yes`。`maximum-scale` なし |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url が 絶対パス | ✅ | 3つ とも `/Typa/`。`"./"` → 絶対パスは 同一性が 変わりません（§4 に 根拠） |
| E2 | アイコン4種 + 透明を ふくまない apple-touch-icon | ✅ | **透明 42.1% → 0.0%** |
| E3 | beforeinstallprompt を head 最上部で 捕捉（外部ファイル） | ✅ | `install-hook.js`（今回 追加）。前は 19本目の `app.js` |
| E4 | インストールボタン（案内できる ときだけ 表示） | ✅ | `hidden` を 合図で 外す。押す 処理は いつも 付ける |
| E5 | 自アプリ接頭辞の キャッシュのみ 削除（実測で 確認） | ✅ | 他アプリの ひかえ **2/2 のこった** |
| E6 | sw.js が localStorage に さわって いない | ✅ | 0件（注意書きの コメントを 除いて 判定） |
| E7 | 更新通知（押すまで 切りかわらない ことを 実測） | ✅ | **前：3秒で 勝手に 切替 → 後：waiting の まま** |
| E8 | 初回訪問で 勝手に リロードしない（画面遷移1回） | ✅ | 文書の 読みこみ **1回**（前も 1回。`framenavigated` は `pushState` で 3回 出ますが 読みこみでは ありません） |
| E9 | Service Worker が 実際に 登録されて いる | ✅ | `getRegistration()` で `active: true` |
| E10 | offline.html（外部資産・JS に たよらない） | ✅ | 今回 追加。**サーバーを 止めて** 実測（`setOffline` は SW に 効きません。§4 参照） |
| E11 | APP_VERSION を 今回の リリース値に 更新 | ✅ | `v18` → `v19` |
| E12 | maskable の セーフゾーン外の 中身 0.2% 以下 | ✅ | **0.000%**。あわせて 下地を はしまで のばした |
| E13 | iOS の「ホーム画面に 追加」手順を MANUAL に 記載 | ✅ | MANUAL.md に あり |

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `<img>` 0件（SVG は `aria-hidden`）。`aria-label` 19か所、トーストは `aria-live="polite"`、更新の帯は `role="status"` |
| F2 | モーダルに role="dialog"・Esc で 閉じる | — | 該当なし（モーダルを つかわず、画面遷移と「もどる」に 統一。Esc も その 経路） |
| F3 | キーボードのみで 全機能に 到達 | ✅ | **測りました（→ §8）。** 12画面を Tab だけで 歩いて 0件。以前は「ファイルを えらぶ」が **マウスでしか 押せません**でした |
| F4 | rt の 色を 決め打ちして いない | — | 該当なし（ルビを つかって いません） |
| F5 | 初回JS 300KB以下 | ❌ | **479 KB**（gzip 後 135 KB）。→ §4 |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 `js/app.js` 2,832行 / 136 KB |

### G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を もたない | ✅ | `localStorage['study.records.v1']`。氏名・出席番号・メールを もたず、送信しない（`tools/check-study.js` で 検査） |
| G2 | 中断記録・5分ルール | ✅ | `status:"aborted"` あり。5分以上の 離席は 離席時刻で 締める（同 検査） |

---

## 4. 測って いない もの／人間に 決めて ほしい こと

**測って いない もの（「未計測」であって ✅ では ありません）**

- **本番（`gigayama.github.io/Typa/`）での 動作。** 作業環境から 到達できません（プロキシ 403）。
  実測は すべて 手もとの 検査用サーバーを `/Typa/` の 形で 立てて 行って います。
  manifest の `scope`/`start_url` を 絶対パスに した ので、**本番で いちど インストールし直して
  たしかめて ください**（同一性は 変わらないので、入って いる 端末で 別アプリには なりません）。
- **実機での 表示。** Chromebook・iPad の 実機は ありません。画面サイズと 明暗で 代用して います。
- ~~**LCP / CLS。** 測って いません。~~ → **§8 で 測りました**（LCP 756ms / CLS 0）。
- ~~**キーボードのみでの 全機能到達（F3）と Tab 順。** 測って いません。~~ → **§8 で 測りました**（0件）。
- **iOS の ホーム画面での アイコンの 見え かた。** 透明 0% は 画素で たしかめましたが、
  実機での 表示は 見て いません。
- **ハイコントラストモードの 実機での 見え かた。** CSS は 入れましたが、
  Windows の コントラストテーマでの 実表示は 見て いません。

**人間に 決めて ほしい こと**

1. **初回JS 479KB（目標 300KB）を どうするか。**
   19本の スクリプトを ぜんぶ 同期で 読んで います。減らすには
   **ビルド（まとめる・小さくする）の 導入**が 要ります。
   これは アーキテクチャの 変更なので、Part III の 停止条件に あたります。
   ここでは **やらずに 提案に とどめて います**。
   （なお 総アセットは 約 642KB で「初回 1MB 以下」は 満たして います。
   gzip 後の JS は 135KB で、GitHub Pages は gzip を かけます。）
2. **提示モード（D12）を つくるか。**
   Typa は 個別練習の アプリなので 一斉授業の 想定では ありませんが、
   ローマ字ひょうを 電子黒板に 映す 使いかたは ありえます。
   機能の 追加に あたるので、`/rollout` では 手を 入れて いません。
3. **ローマ字ひょうの 印刷（D13）を つくるか。**
   いまは 固定要素を 隠すだけです。紙面として 整える なら 別途 設計が 要ります。
4. **`js/studyLog.js` の 正本を どの リポジトリに 置くか。**
   8アプリで 中身が 同じ ファイルです。Typa 側では 変更して いません。

---

## 5. ほかの リポジトリにも 効く 知見

`/rollout` の 横断修正に つかえる もの。

1. **`opacity` を 行や ボタンに かけると、字まで うすく なる。**
   「つかわない ものを 目立たせない」つもりが「読めなく する」に なります。
   `grep -rn "opacity: \.[0-8]" css/` で ひろえます。Typa では これが 最悪値（2.48）でした。
2. **`#fff` で 決め打ちした 文字色は、くらい 画面で 落ちる。**
   明暗で 色を ふりなおして いる パレットでは、
   「みどり・あかの 上の 白」が 1.58 / 2.21 に なります。
   `grep -rn "color: #fff" css/` で ひろえます。
3. **`style-src 'self'` は `style="--x: …"` を 止める。**
   CSS 変数を インラインで 出して いる アプリでは、
   **画面は 出るのに 色分けだけ 黙って 消えます。**
   `grep -rn 'style="--' js/` が 1件でも あれば、CSP を 入れる まえに 確認を。
4. **`caches.keys()` の 検査は `startsWith` を ファイル 全体から さがしては いけない。**
   `sw.js` の `fetch` には 別の `startsWith` が ふつうに あります。
5. **`offline.html` の 検査は「先読みの 配列に あるか」で 見る。**
   逃げ道の `caches.match('./offline.html')` だけでも 通って しまいます。
6. **Playwright の `setOffline` は Service Worker に 効かない（Chromium）。**
   圏外の 検査は **サーバーを 止めて** 行わないと、意味の ない 結果に なります。
7. **`package.json` に `"type": "module"` を 足すと、既存の CommonJS の 検査が 全滅する。**
   Typa では `tools/check-*.js` 6本 とも 動かなく なりました。

---

## 6. 追補（2026-08-08）— 「タイパ」の たてまえと 中身が 合って いるかの 見なおし

このアプリの 名まえは「**無駄なく、隙間時間にも 効果的に 学習できる**」から 来て います。
その たてまえが コードで ほんとうに 守られて いるかを、
**ぜんぶの JS（19本・16,408行）を 読み**、実ブラウザで はかって たしかめました。

### 6-0. 見なおしの しかた（そのまま くりかえせます）

```bash
npm run check                                   # 静的な 検査（38件）
node tools/check-progress.js                    # ひとまわりの つみあげ（202件）
node tools/check-backup.js                      # もちだし（56件）
npm i -D playwright@1.56.0 && npm run measure    # 見た目（4画面 × 明暗）
npm run measure:typing                          # 打って いる とちゅう
npm run measure:pwa                             # 更新・圏外・他アプリの ひかえ
```

> Playwright は **入って いる Chromium の 版に あわせて** えらびます。
> ここでは `chromium-1194` だったので `playwright@1.56.0` です。
> 新しすぎる ものを 入れると `Executable doesn't exist at …chromium_headless_shell-1234` で 止まります。

### 6-1. 直した もの（4件）

| # | どこ | どう なって いたか | 気づけたか |
|---|---|---|---|
| 1 | `js/shortcut.js` | 課題が できた **0.26びょう あと**に つぎの 課題を 出します。その あいだに「もどる」を おされると 画面は もう けっか画面で、`$('sc-task').innerHTML` が **null への 代入**に なって 例外で 止まって いました | ❌ 実ブラウザで 再現。`TypeError: Cannot set properties of null` |
| 2 | `js/app.js` `celebrate()` | ひとまわりして いない 回の ★は `starsOf()` の 見つもりで、そこには「みじかい ひとまわりでも ミス1かいまでは ゆるす」下ささえが きいて います。**5打 打って 1回 まちがえただけの 回**も ★3つ あつかいに なり、★を 1つも 出して いない けっか画面で ひらひらだけが まって いました | ❌ 実ブラウザで 70こ 確認 |
| 3 | `js/backup.js` | ヒントの つよさ（`assist`）は きほんが `'custom'`（文字）ですが、えらぶと **0〜3 の 数**に なります。型だけを 見て いたので、端末を うつすと その 数が だまって おちて いました。「じどう」に して いた 子は、**おぼえぐあいに あわせて ヒントを 下げる しくみが 止まります** | ❌ 書き出し → 読みこみで `assist: 2` → `undefined` |
| 4 | `js/app.js` `recommend()` | 上と 同じ 見つもりで、3打で やめた 回にも「ばっちり 打てて います。めかくしで やってみよう」を 出して いました | 読んで 発見 |

1・2・3 は **回帰検査を 足しました**（`tools/check-backup.js` に 15件、
`tools/check-progress.js` に 8件）。3 は 直す 前の コードに かけると 8件 落ちます。

### 6-2. 速く した もの — 同じ きろくを 何度も 読み直して いた

`js/store.js` の `read()` は よばれる たびに `JSON.parse` を 頭から やり直して いました。
きろくの ならび（`typa.history.v1`）は 300回ぶんで **176KB** です。ところが

- 「にがて」の ひきだし … 1回 出すのに **6回**（ミス集計・おぼえぐあい・ローマ字の きまり・ふたたび）
- れんしゅうを はじめる とき … `stageMastery()` で 1回（＋ すすみぐあいを 5回）

読み直して いました。**打ちはじめるまでの 待ち時間**は このアプリが いちばん
みじかく したい ところ です。

読んだ 結果を「**その ときの 文字列と 一組で**」おぼえ、読むたびに 文字列を
見くらべます。1文字でも ちがえば 読み直すので、`backup.js` が localStorage に
じかに 書いても、べつの タブが 書きかえても、**古い ものが 返る ことは ありません**
（`tools/check-progress.js` の 11番で 検査）。

| 画面 | 前 | 後 |
|---|---:|---:|
| 「にがて」の ひきだしを 1回 出す | 6.49 ms | **0.45 ms** |
| れんしゅうを 1回 はじめる | 1.35 ms | **0.12 ms** |

（Node・きろく 300回ぶん・30回の 平均。学校の Chromebook では この 数倍に なります）

### 6-3. はかった もの — ひらいてから 打つ 画面が 出るまで

CPU を 4ばい おそくした 実ブラウザ（Chromebook に よせた もの）で はかりました。

| | ms |
|---|---:|
| HTML が とどく | 14 |
| JS 20本が ぜんぶ とどく（通信の 合計 70ms） | 274 |
| **DOMContentLoaded（＝19本の 実行が おわる）** | **826** |
| はじめて 字が 出る | 204 |

**通信では ありません。とどいて からの 550ms が まるごと JS の 読みこみ・実行です。**
§4 の F5（初回JS 479KB／目標 300KB）が、いま のこって いる いちばん 大きい
「タイパ」の むだ だと はっきり しました。**これを 減らすには まとめる 仕組み（ビルド）が
要り、アーキテクチャの 変更に あたる**ので、ここでも 手は 入れて いません（§4-1 の まま）。

### 6-4. たてまえが 中身で 守られて いた ところ（読んで たしかめた もの）

| たてまえ | どこで 守られて いるか |
|---|---|
| ひらいたら すぐ 打てる（0タップ） | `nav.js` の `ROOT_TAB = 'play'`。`app.js` の `renderPlay()` は 行き先が 空でも じぶんで 決める |
| どの 画面からでも すぐ 打つ 画面へ | `bindPlayShortcut()`（スペースキー）。文字入力・チェックボックス・たたむ 見出しの 上では 打ちけす |
| 10びょうでも むだに ならない | `store.js` の `lapAdvance()`。打った お題は `lapItems` に たまり、`play.js` の `firstQueue()` が **とちゅうの お題から** 出す |
| とじても 消えない | `play.js` / `shortcut.js` の `pagehide`（タブを すてられる Chromebook むけ）|
| 待った 時間は 学習時間に しない | 1打目から 時計を うごかす・5びょう こえた 手止まりを ぬく・タブが 見えない あいだは 止める・**5分こえたら はなれた 時こくで 締める** |
| 忘れる 前に もどす | `dueStages()`（1→3→7→14→30日）を「つぎに やる こと」の **いちばん 先**に 置く |
| その子の 弱点だけを 打つ | `mastery.js` の `weakTargets()`。**まちがえないが 手が とまる キー**と **とりちがえた くみあわせ** から お題を 組む |
| まちがえた ぶんの 打ち直しで 成績が 上がらない | `play.js` の `counts = state.phase === 'main'`。おまけの 周は はやさ・正かくさ・★・けいけんち から ぬく |

### 6-5. のこって いる こと

1. **初回JS 479KB（§4-1 の まま）。** 上の 実測で「通信では なく 実行が 550ms」と
   はっきり したので、判断の 材料は そろって います。
2. **キーボードのみでの 全機能到達（F3）。** 前回に つづき 未計測です。

---

## 7. 追補（2026-08-08 その2）— 初回JS 479KB を けずって みて、**それでは 速く ならない**と 分かった

§4-1 で 人の 判断に まわして いた「初回JS 479KB（目標 300KB）」を、
いちど **実さいに けずって はかりました**。けっか、**量は 原因では ありません**でした。
判断の 材料に なるので、やった ことと 数字を のこします。

> **けっきょく 入れて いません。** リポジトリは いまも「ビルドなし」の ままです（A型）。
> 下の 数字が その 理由です。

### 7-1. ためした こと

`js/*.js` を `src/*.js` に うつし、**コメントと 字下げだけを 外した もの**を
`js/` に つくる 形を 組んで、まるごと 動かしました
（名前は 1つも 書きかえない ので、配信用も そのまま 読めます）。

- **480 KB → 262 KB（45% 減）**。「初回JS 300KB以下」は 満たせました
- 配信用が ソースと ずれない ように、その場で 作り直して バイトで くらべ、
  さらに src/ と js/ を べつべつに 読みこんで **同じ こたえを 返すか**まで
  突き合わせる 検査（125件）も 組みました

### 7-2. はかった こと — 45% けずって 縮んだのは 11ms（1%）

CPU を 4ばい おそくした 実ブラウザ（Chromebook に よせた もの）で、
**ひらいてから 打鍵を うけつけられる ように なるまで**を 10回ずつ はかった 中央値です。

| | けずる前 | けずった後 |
|---|---:|---:|
| 19本の JS の 実行が おわる | 326 ms | **315 ms** |
| 打つ 画面が えがかれる | 964 ms | 931 ms |
| **打鍵を うけつける** | **986 ms** | **954 ms** |

**JS の 量が きいて いたのは 11ms だけ**でした。
V8 は コメントを 読みとばすのが 速く、関数の 中身も 呼ばれるまで 組み立てません。
**479KB という 数字は 大きく 見えますが、そのまま 時間には なりません。**

これが、この 仕組みを 入れなかった 理由です。1% の ために
「ソースと 配信用が 2つ ある」状態と、build を わすれる 事故を
ずっと かかえる ことに なります。

### 7-3. では 何に つかって いるのか（内わけ）

同じ 実ブラウザで サンプリングしました。

| どこ | ms（CPU 4ばい おそく） | わりあい |
|---|---:|---:|
| JS 19本の 通信 | 70〜80 | 8% |
| JS 19本の 読みこみ・実行 | 約 240 | 25% |
| **打つ 画面を 組み立てて、はじめて 1回 レイアウトする** | **約 640** | **65%** |
| 　うち `fitKeyboard()`（キーボードの 大きさを 実さいに はかる） | 約 215 | 22% |
| 　うち 80キーの グリッドと SVG 2つの 初回レイアウト | 約 480 | 50% |

`fitKeyboard()` の 呼び出しを 外すと 954ms → **748ms** でした（実測）。
`--kb-em` を 2回 ためして 高さを はかる ところで、`getBoundingClientRect` が
**1回 93ms** かかって います。em を かえると キー 80個の 文字の 大きさが
かわるので、そのたびに 文字を 組み直す ことに なるためです。

### 7-4. のこした 手なおし（`js/play.js` の `fitKeyboard`）

**見た目は 1ピクセルも 変えて いません。**

- `--kb-em` / `--kb-row` を `#play-root`（打つ 画面ぜんたい）では なく
  **`.play-lower`（キーボードと 手の 絵だけ）**に 書く ように しました。
  CSS変数を 書きかえると その 下の 見た目を ぜんぶ 計算し直すので、
  まえは お題・メーター・ちびキャラまで 巻きこんで いました。
  この 2つの 変数を 見るのは `.kb-fit` / `.play-visual` / `.kb-key` だけです
- はかる ものを **先に ぜんぶ 読んで から** 書く ように しました
  （「書いて → 読んで」を まぜると、読む たびに その場で 組み直します）

実測 **986ms → 954ms**（10回の 中央値）。

**同じ 見た目に なる ことの たしかめかた**（そのまま くりかえせます）:
6つの 画面サイズ（1366×768 / 1280×800 / 1920×1080 / 810×1080 / 320×568 /
1366×500）× 6つの せってい（きほん・文字を 大きく・手の絵なし・キャラなし・
US配列・ヒント最小）の **36とおり**で、キーボードの 文字の 大きさ・キーの 高さ・
キーボードと 手の 絵の 位置と 大きさを くらべ、**すべて 1ピクセルも
変わって いない**ことを たしかめました。

### 7-5. のこって いる こと

1. **初回JS 479KB（F5）は ❌ の まま**です。§7-2 の とおり 速さには つながらず、
   満たすには「ソースと 配信用を 分ける」形が 要ります。
   **数字を 満たす ためだけに 入れる ものでは ない**と 判断しました。
2. **打つ 画面の はじめの レイアウト（約 640ms）。** いちばん 大きい ところ ですが、
   へらすには キーボードの 組みかた（60列の CSS グリッド）を かえるか、
   `fitKeyboard` を やめて CSS の コンテナ単位（`cqh`）で 高さを 決める
   —— **見た目の 作り直し**が 要ります。手を 入れて いません。
3. **キーボードのみでの 全機能到達（F3）。** 前回に つづき 未計測です。

---

## 8. 追補（2026-08-08 その3）— ずっと「未計測」だった F3 と LCP/CLS を 測った

§4 と §6-5 と §7-5 で、3回 つづけて「未計測」と 書いて きた ものが 2つ ありました。

1. **キーボードのみでの 全機能到達（F3）と Tab 順**
2. **LCP / CLS**

どちらも「あとで」と 書いた まま 残って いた ところです。**測りました。**
**片方から 本物の 不具合が 1件 出ました。**

### 8-1. 見つかった もの — 「ファイルを えらぶ」に キーボードでは 一生 たどりつけなかった

`きろくを もちだす` 画面の **読みこむ** の ところです。

```html
<!-- 前 -->
<label class="btn btn-outline" for="bk-file">ファイルを えらぶ</label>
<input type="file" id="bk-file" accept="application/json,.json" hidden>
```

`<label>` は それ自身 Tab の 順に 乗りません。指す 先の `<input>` に `hidden` が
ついて いた ため、**この ボタンは マウス（か タッチ）でしか 押せません**でした。

| | |
|---|---|
| どう 見えるか | ふつうの ボタンに 見えます。マウスなら ちゃんと 動きます |
| 何が できないか | **キーボードだけで 使う 人は、書き出した きろくを 読みこむ 手が まったく ありません** |
| なぜ 気づけなかったか | ビルドも 静的検査も 通り、画面も ふつうに 出ます。**歩いて みるまで 分かりません** |
| 誰が 困るか | マウスの 使えない 児童・先生。端末を 入れかえる ときに いちばん 要る 機能です |

**直しかた。** 見えなく するのを やめたのでは ありません。
`hidden`（＝ `display:none`）を やめて、**見た目だけ 消して Tab には のこす**形に しました。
せっていの スイッチが もともと 使って いた 形と 同じです。

```html
<!-- 後。input が 先。フォーカスの しるしは となりの ラベルに 出します -->
<input type="file" id="bk-file" accept="application/json,.json" class="file-pick-input">
<label class="btn btn-outline" for="bk-file">ファイルを えらぶ</label>
```

```css
.file-pick-input { position: absolute; opacity: 0; width: 0; height: 0; }
.file-pick-input:focus-visible + .btn { outline: 3px solid var(--primary); outline-offset: 2px; }
```

**見た目は 1ピクセルも 変わって いません**（`position: absolute` なので 流れに 入りません）。

**直った ことの たしかめかた**（実測）

| | 前 | 後 |
|---|---|---|
| Tab で `#bk-file` に 届くか | **届かない**（30回 押しても 来ない） | **Tab 2回で 到達** |
| フォーカスの しるし | （届かないので 無し） | ラベルに `3px solid rgb(74,144,226)` |
| 書き出し → 読みこみ が 通るか | 通る（マウス） | 通る（`setInputFiles` で 確認、JSエラー 0件） |

### 8-2. F3 の 測りかた（`scripts/measure/keyboard.mjs`。新しく つくりました）

```bash
npm run measure:keyboard
```

12画面（うつ／えらぶ／コース一覧／チャレンジ／きろく／バッジ／ローマ字ひょう／
きろくを もちだす／せってい 4タブ）を、**実さいに Tab を 押して 歩きます。**

| 測る こと | 結果 |
|---|---:|
| 押せそうなのに Tab で 届かない | **0件**（前 1件） |
| 見えて いるのに Tab 順に 出て こない | 0件 |
| Tab の 順が 見た目の 順と ちがう | 0件 |
| フォーカスの しるしが 出ない | 0件 |
| Tab が ひとまわり しない（わな） | 0件 |
| JSエラー・CSP違反 | 0件 |

### 8-3. ⚠️ 検査を 書く ほうに 出た 誤報 3種（測る 道具も 測る）

はじめに 走らせた ときは **24件** 出ました。**そのうち 23件は 検査側の まちがい**でした。
「0件でした」と 同じくらい、「たくさん 出ました」も そのままでは 信じられません。

| 誤報 | 何が おきて いたか | 直しかた |
|---|---|---|
| スイッチの 見た目 12件 | `cursor: pointer` は **継承します**。`<label><input><span></span></label>` の span まで「押せそうなのに 届かない」と 数えて いました | 囲みの `<label>` の 中に 本体が あれば 除く |
| 「順ちがい」3件・「わな」2件 | ヘッドレスには アドレスバーが 無いので、さいごまで Tab を 押すと **先頭へ もどって きます**。それを「上へ 逆もどりした」と 読んで いました | 同じ ところに 2回 来たら ひとまわりと みなして 止める |
| （8-4 の 大きさ 0 KB） | サーバーが `Content-Length` を 出して いなかった ため、**ぜんぶ 0 KB → 「0 KB なので 300 KB 以下、合格」**と いう うその ✅ を 出して いました | `PerformanceResourceTiming` の `decodedBodySize` で 数える。0 バイトは **合格に しない** |

3つめは とくに たちが 悪い 形です。**「測れて いない」が「良い 数字」に 化けて いました。**

### 8-4. LCP / CLS の 測りかた（`scripts/measure/perf.mjs`。新しく つくりました）

```bash
npm run measure:perf
```

まっさらな プロフィールで 5回ずつ ひらいた 中央値です（CLS だけは いちばん 悪い 回）。

| | LCP | CLS | はじめて 字が 出る | DOMContentLoaded |
|---|---:|---:|---:|---:|
| ふつう | 256 ms | 0 | 76 ms | 203 ms |
| **Chromebook風（CPU 4ばい おそく）** | **756 ms** | **0** | 156 ms | 667 ms |

| 目標（§8） | 目標値 | 実測（Chromebook風） | |
|---|---:|---:|:--:|
| LCP | 2,500 ms 以下 | **756 ms** | ✅ |
| CLS | 0.1 以下 | **0** | ✅ |
| 総アセット（初回） | 1 MB 以下 | **632.7 KB** | ✅ |
| 初回JS（gzip 前） | 300 KB 以下 | **491.9 KB** | ❌ |

内わけ：JS 491.9 KB ／ CSS 130.8 KB ／ 画像 0.6 KB ／ HTML ほか 9.4 KB。

> ⚠️ **本物の Chromebook の 数字では ありません。** CPU を 4ばい おそく した 目やすです。
> 実機は 手もとに ありません。§7 の 測りかたと そろえて あります。

**F5（初回JS）だけが ❌ の まま**です。§7-2 で
「45% けずっても 速く なったのは 11ms（1%）」と 測って あり、
LCP・CLS が 目標の 3分の1 で 収まって いる ことも 今回 分かった ので、
**数字を 満たす ためだけに ビルドを 入れる 判断は しません**（§7-1 の まま）。

### 8-5. もどらない ように した こと

| 入れた もの | 何を 止めるか |
|---|---|
| `F_LABEL_FOR_TABBABLE`（品質ゲート 39件目） | `<label for>` の さす 部品に `hidden` / `display:none` が ついたら 落とす |
| `scripts/self-test.mjs` に こわしかた 追加（33件目） | 上の 検査が **こわしたら ほんとうに 落ちる** ことを 毎回 たしかめる |
| `npm run measure:keyboard` | 画面を 足した ときに、Tab で 届かない ものが 増えて いないか |
| `npm run measure:perf` | LCP・CLS・大きさ |

### 8-6. ⚠️ 測る 環境の 落とし穴（他の リポジトリでも 踏みます）

**書き出した ファイルの 名前が `download` に なる**、という 現象を 見ました。
`typa-きろく-2026-08-08.json` と 付けて いるのに、です。

追いかけた 結果、**アプリの 不具合では ありませんでした。**
測って いる コンテナの ロケールが `POSIX`（UTF-8 でない）だった ため、
Chromium が 日本語の ファイル名を 表せず、`download` に 落として いました。

| ロケール | `download="typa-きろく-….json"` |
|---|---|
| （なし・POSIX） | `download` に なる |
| `C.UTF-8` | `typa-きろく-2026-08-08.json` の まま |

Chromebook・iPad・Windows は UTF-8 なので、**実機では 起きません。**
**ここで ファイル名を ローマ字に 直して いたら、直す 必要の ない ものを
児童に 見える 形で 変えて いました。** 環境の せいか アプリの せいかは、
**切り分けて から** 手を 入れます。

### 8-7. 🆕 ブラウザが ダウンロードできない ネットワークでも 測れる ように した

`cdn.playwright.dev` へ 出られない 環境では、**手もとに Chromium が あっても
playwright が 版ちがいだと 使って くれず**、実測が まるごと できません（403 で 止まります）。
§7-4 の「CDN が 塞がれた 環境で 測ると 数字が 意味を 失う」と 同じ 形の 事故です。

`scripts/measure/launch.mjs` を 足して、5本 とも 場所を 教えられる ように しました。

```bash
CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npm run measure
```

### 8-8. のこって いる こと

1. **初回JS 491.9 KB（F5）は ❌ の まま。** §7-2・§8-4 の とおり、
   速さには つながらないと 測れて います。判断は 人に まわして あります。
2. **実機（Chromebook・iPad）での 表示と ふるまい。** 手もとに ありません。
3. **本番（`gigayama.github.io/Typa/`）。** プロキシ 403 で 到達できません。
4. **ハイコントラストモードの 実表示。** CSS は 入れて ありますが 見て いません。
